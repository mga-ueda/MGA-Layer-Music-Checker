# MGA Layer Music Checker

多レイヤー音声（最大 4 トラック）の同期再生・ミックス・スペクトラム／メーター表示をブラウザ内だけで行う Web アプリです。音声ファイルはサーバーへ送信されません。

**[▶ オンラインで使う（GitHub Pages）](https://mga-ueda.github.io/MGA-Layer-Music-Checker/)**

| | |
|---|---|
| **GitHub Pages** | [https://mga-ueda.github.io/MGA-Layer-Music-Checker/](https://mga-ueda.github.io/MGA-Layer-Music-Checker/) — ダウンロード不要ですぐに利用できます |
| **バージョン** | v1.02 |
| **ローカル起動** | リポジトリの `index.html` をブラウザで開く（`css/`・`js/` を同フォルダに配置・ビルド不要。ローカル HTTP サーバー推奨） |
| **推奨ブラウザ** | Google Chrome |
| **リポジトリ** | [mga-ueda/MGA-Layer-Music-Checker](https://github.com/mga-ueda/MGA-Layer-Music-Checker) |
| **ライセンス** | MIT License（全文は [LICENSE](LICENSE)） |

<!-- manual-body-start -->

## 特記事項

- 本ページは**無償で提供される実験的なウェブアプリ**です。不具合を常時監視しておらず、ご利用中に問題が生じても原則として把握いたしません。あらかじめご了承のうえご利用ください。
- 音声は**このブラウザ内でのみ**デコード・再生され、サーバーへは**いかなる情報もアップロードされません**。**Google Chrome** の利用を推奨します。ブラウザや形式によってはデコードできないことがあります。
- このページの **HTML・CSS・JavaScript** は、目的を問わず**自由に改変・複製・再利用**して構いません（商用・社内利用を含みます）。事前の連絡も不要です。詳細は「改変・再利用」「ライセンス」を参照してください。
- 本アプリは静的ファイル（`index.html`・`css/`・`js/` など）で構成されています。**ビルドやサーバーは不要**です。フォルダごと保存し、ルートの `index.html` をブラウザで開いてください（`file://` で動作します。favicon 等の外部 CDN 参照は環境により取得できない場合があります）。
- スペクトラムアナライザーは特定の計測器や製品仕様の再現を目的としたものではありません。制作者の好みの挙動に寄せていますが、帯域検波・準ピーク検波・キャリブレーションは Web Audio の AnalyserNode と独自後処理の組み合わせであり、商用アナライザの代替にはなりません。
- ディスク上の元ファイルを編集したり書き出しし直した場合でも、本アプリは**自動では再読み込みしません**。更新した音声を反映するには、**ドロップゾーン**へ再度ドロップしてください。前回セッションの復元はブラウザ内の保存データ（`IndexedDB`）に保存されたコピーを参照します（元ファイルのパスは保持しません）。ブラウザのストレージ整理により保存データが消えると、前回のトラックは復元できません。
- **Spectrum floor** / **Level meter floor** / フェード時間（秒）などの UI 設定は、次回開いたときにブラウザ内（`localStorage`）から復元されます。読み込んだ音声データのコピーもブラウザ内（`IndexedDB`）に保存されます。

## 使い方

### ブラウザとプライバシー

- Google Chrome の利用を推奨します。ブラウザによっては形式ごとにデコードできないことがあります。
- 音声はこのブラウザ内でのみデコード・再生され、いかなる情報もサーバーへ送信されることはありません。

### ファイルの読み込み

- 破線のドロップゾーンに WAV / MP3 / AIF（AIFF）をドラッグ＆ドロップします。複数トラックを同時に選んでドロップすると、最大 4 トラックまで、ファイル名ソート順でスロット 1〜4 に読み込まれます。1 ファイルだけドロップした場合はスロット 1 のみです。複数スロットを使うには、OS のファイル一覧で複数選択してから一度の操作でドロップしてください。
- ディスク上の元ファイルを編集したり書き出しし直した場合でも、本アプリは自動では再読み込みしません。更新した音声を反映するには、必ずドロップゾーンへドロップし直してください。次回起動後に PLAY だけで戻るのは、前回保存されたデータのコピーです（保存の仕組みは特記事項を参照）。

### 再生・ミキシング・表示

- 読み込み成功後、スロット 1 のフェーダーは MAX に設定され、同期再生が自動で開始します。スロット 2〜4 はフェーダーを上げるまで最小のままです。
- PLAY SYNC ボタンで再生の開始／停止。再生中は全トラックが同期され、シークバーと時間の最大値は、読み込んだトラック内で最短の長さのものが利用されます。
- 縦型のフェーダーで各チャンネル音量を設定します。Fade IN / OUT は、トランスポート行の フェード時間（1〜30 秒の整数のみ）をかけてフェーダーを変化させます。フェード中に同じスロットへ再度フェードをかけた場合は、直前のフェードに使っていた秒数を切り上げて半分にした長さ（最短 1 秒）で新しいフェードを行います（フェードが加速するような挙動になります）。この操作によるデフォルト値の上書きはされません。
- 画面上の Spectrum floor / Level meter floor / トランスポート付近の フェード時間（秒）で、スペクトラム／メーターの表示下限やフェード長を変更できます。
- クリップが検出されると、自動的にマスターボリュームが減衰し、クリップ表示が点灯します。マスターボリューム値は、次回起動時には 100% にリセットされます。

### キーボードショートカット

- キーボードショートカットが利用できます。テキスト／数値入力にフォーカスがある間はキーボードショートカットは無効です。アプリ画面上のキーボードショートカット表を参照してください。

### データの保存とパフォーマンス

- 各スロットの音量・Mute・Solo などのミックス状態と、読み込んだ音声のコピーは、ドロップ直後およびミックス操作のあと IndexedDB に保存され、次回以降 PLAY で前回トラックを復元するときに使われます（HTTP のディスクキャッシュではなく、このアプリ用の保存領域です）。
- スペクトラム表示下限・レベルメーター表示下限・フェード時間（秒）を変更すると、ブラウザのローカルストレージに保存され、次回このページを開いたときに復元されます。
- ドロップで読み込んだ音声は、その時点のファイル内容がブラウザの IndexedDB にコピーされ保存されます。あとから PLAY で前回セッションを復元するとき参照されるのはこのコピーであり、ドロップ元のファイルパスは保持せず、ディスク上の元ファイルを自動で読み直すこともありません。<span class="app-manual-r255">DAW 等で波形・音声を更新したあとに本アプリへ最新版を反映したい場合は、必ずファイルをドロップし直してください。</span>ブラウザのストレージ整理や設定により IndexedDB が消えると、前回のトラックは復元できません。
- データ量の多いトラックを複数同時に扱うと、ブラウザのメモリ使用量が増え、操作や描画の応答が遅くなることがあります。長時間の連続再生のあとも同様です。タブをバックグラウンドに置いたり、ブラウザや OS の省電力・省リソースの働きでタブの優先度が下がったりすると、音声の再生そのものは続く場合でも、タイマーや `requestAnimationFrame` の更新頻度が下がり、メーターやシークバーなど画面表示の追従が鈍くなることがあります（ブラウザ仕様によるものです）。表示を確実に追いたいときはタブを前面に戻すか、一度ページを閉じて開き直してください。

## 改変・再利用

- このページの **HTML・CSS・JavaScript** は、目的を問わず**自由に改変・複製・再利用**して構いません（商用・社内利用を含みます）。事前の連絡も不要です。
- ソースコードは [GitHub リポジトリ（mga-ueda/MGA-Layer-Music-Checker）](https://github.com/mga-ueda/MGA-Layer-Music-Checker) で公開しています（**Code** → **Download ZIP** または **git clone** で取得できます）。
- **画面上のフッター**（© 2026 の表記と会社サイトへのリンク）は、独自版では**削除しても構いません**。未改変の配布では残すことを推奨しますが、必須ではありません。
- **ソースを第三者に配布・公開する場合**は、配布物に [LICENSE](LICENSE) ファイルを含めてください（MIT License の条件です）。フッターを削除しても、`LICENSE` の同梱は必要です。

## ライセンス

- 本リポジトリのソース（`index.html`・`css/`・`js/` など）は **MIT License** で公開しています。
- **通常の利用**（ブラウザで開いて使うだけ）に、ライセンス表記の対応は不要です。
- **ソースの配布・フォーク**のときは、[LICENSE](LICENSE)（英文）を配布物に同梱してください。`Copyright (c) 2026 MIYABI GAME AUDIO INC.` および利用許諾の条文が含まれています。
- **画面上のフッター**と `LICENSE` ファイルは別です。フッターは表示上の出典表記であり、削除しても MIT の同梱条件は変わりません。
- MIT License では、改変・複製・配布などが認められます。ソフトウェアは現状有姿（「AS IS」）で提供され、作者による保証はありません。条文の全文は [LICENSE](LICENSE) を参照してください。

## バージョン情報

### v1.02 - 2026年5月14日

- マスターボリュームの％表示に加え、線形ゲインを 100%=0 dB とした振幅比 dB（`20·log10(g)`）を併記。ゲイン 0 のときは理論値として `−∞ dB` と表示。

### v1.01 - 2026年5月13日

- 長時間使用時のメモリ滞留対策：スロット上書き／全リセット時に旧 `GainNode`・`AnalyserNode` を `disconnect()` で音声グラフから切り離すよう変更。再ドロップを繰り返した際に古いノードが `AudioContext` 内部に残り続ける問題を抑制。
- ログパネル肥大化対策：表示行数の上限（`LOG_MAX_LINES` = 500 行）を設け、超過時は古い行から自動削除するよう変更。
- 5 ファイル以上ドロップされた場合、5 個目以降が無言で捨てられていた挙動を変更。読み込み対象外となったファイル名をログに表示するように。

### v1.00 - 2026年5月12日

- 初稿（初版リリース）

<!-- maintainer-only: 本ファイル編集後は py -3 scripts/sync-manual.py で index.html と README.md を更新 -->
