{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Sync manual (docs → index.html & README)",
            "type": "shell",
            "command": "py",
            "args": ["-3", "scripts/sync-manual.py"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}
